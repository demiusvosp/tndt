{% for option in filter.options %}
<label class="form-check">
    <input class="form-check-input" type="checkbox" name="stage" value="{{ option.value }}">
    <span class="form-check-label">{{ option.label }}</span>
</label>
{% endfor %}